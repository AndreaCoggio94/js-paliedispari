const userWord = prompt("Inserisci una parola");

palindroma(userWord, userWord.length);

function palindroma(word, length) {
  console.log(word);
  console.log(typeof word);
  console.log(length);
  console.log(typeof length);
}
